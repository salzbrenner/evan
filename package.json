{
  "name": "evan-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "style-dictionary": "pnpm --filter ./packages/tokens run build",
    "dev": "pnpm -r --parallel run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
