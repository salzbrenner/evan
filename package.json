{
  "name": "evan-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "sd": "pnpm --filter ./packages/tokens run build",
    "dev": "pnpm -r --filter !./packages/lab --parallel run dev",
    "ui": "pnpm --filter ./packages/ui dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
